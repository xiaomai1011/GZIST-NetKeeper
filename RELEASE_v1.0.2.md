# v1.0.2 — 修复「缺少配置时守护会静默挂起」

本次只修一个缺陷，**登录、探测、保活逻辑与 v1.0.1 完全一致**，没有功能变化。

## 修的是什么

`campus_net.ps1` 里「检查配置文件」这一步排在「进入守护模式」之前。当脚本所在目录**没有** `campusnet_config.json` 时，它会进入**交互式**配置问答，站在那里等用户输入学号和密码。

问题在于：后台运行和开机自启都是用 `silent_start.vbs` **静默拉起**的，**没有任何人能回答这个提问**。结果就是——

- 进程**永久挂起**：占着内存，什么都不做，网络掉了也不会重连
- **一行日志都不写**：连 `campusnet_log.txt` 都不会生成，所以从表面看「守护好像在跑」
- 单实例互斥锁是在守护模式**内部**才获取的，挂起期间根本拿不到锁，于是可以同时存在好几个这样的僵尸进程

## v1.0.2 的行为

后台模式（`-Watch`）遇到缺少配置时，**直接拒绝启动**，先往日志写两条 ERROR（第一条带上它实际使用的配置文件的**完整路径**），然后退出：

```
[2026-09-18 13:20:01] [ERROR] 未找到配置文件: D:\Downloads\校园网自动登录\campusnet_config.json
[2026-09-18 13:20:01] [ERROR] 保活守护拒绝启动(后台模式无法交互式配置), 请双击[重新配置账号.bat]完成配置后再启动
```

「没在保护」这件事因此一眼可见，也能立刻看出它在哪个目录里找配置。手动模式（`重新配置账号.bat`、`校园网登录.bat`）行为不变，照旧引导配置。

## 升级方式

- **老用户**：用新版 `campus_net.ps1` 覆盖旧文件即可，其它文件不用动；也可以直接解压新包，再把原来的 `campusnet_config.json` 拷进去
- 升级后建议双击一次 `安装开机自启.bat`，它会按**当前所在文件夹**重写开机自启路径

## 顺手记下两条踩坑经验

- **从哪个文件夹配置的账号，就要从哪个文件夹安装自启 / 启动守护。** 配置和日志都存放在**脚本自己所在的目录**。把「实际使用的部署副本」和「git 克隆副本」搞混，就会得到「守护在跑、却永远连不上」的效果——本次故障就是这么来的。
- 想确认守护真的在干活：打开同一目录下的 `campusnet_log.txt`，看是否有 `[OK] 保活守护已启动`，掉线后是否有 `[OK] 登录成功！`。
- 开着 VPN（尤其 **TUN 模式**）会破坏掉线检测与自动重连，测试时请先关掉。

## 校验

- 改动范围：`campus_net.ps1` 入口处的配置检查分支（新增后台模式守卫），其余逻辑逐字节未动
- 压缩包内文件与 v1.0.1 相同（17 个），仅 `campus_net.ps1` 更新
- `README.md` / `index.html` 本次未改动
